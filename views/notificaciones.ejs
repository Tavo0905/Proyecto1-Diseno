<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Notificaciones</title>
    <link rel="stylesheet" type="text/css" href="notificacion.css">
</head>

<body>
    <form action = "" method = "POST">
        <button class = "salir" id = "btnSalir" name = "btnSalir" formaction = "salirNotifEst">
            Salir
        </button>
    </form>
    <div class="container">
        <h1>Notificaciones</h1>
        <ul>
            <% notificaciones.forEach(function(notificacion) { %>
                <li <% if (notificacion.read) { %>class = "leido"<% } %>>
                    <%= notificacion.mensaje %>
                        <form action = "" method = "POST">
                            <div class = "containerBtns">
                                <% if (!notificacion.leido) { %>
                                    <button class = "notifBtn" formaction = "marcarLeido">Marcar como leída</button>
                                <% } %>
                                    <button class = "notifBtn" formaction = "delNotif">Eliminar</button>
                            </div>
                        </form>
                </li>
                <% }); %>
        </ul>
    </div>
</body>

</html>